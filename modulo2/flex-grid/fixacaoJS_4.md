```
function contaOcorrencias(arrayDeNumeros, numeroEscolhido) {
  let n = 0

  for (let number of arrayDeNumeros){
if (numeroEscolhido === number) n++}


    if (n === 0) {return `Número não encontrado`}
    else
  return `O número ${numeroEscolhido} aparece ${n}x`}
```
